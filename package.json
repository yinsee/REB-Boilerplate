{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.10"
  },
  "main": "src/electron-starter.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "electron-test": "ELECTRON_DEV=1 electron .",
    "electron-build": "react-scripts build && electron-packager ./ --overwrite"
  },
  "homepage": "./",
  "devDependencies": {
    "electron": "^1.6.11"
  }
}
